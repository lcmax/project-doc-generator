---
name: project-doc-generator
description: 自动分析项目代码并生成软件工程全生命周期文档套件（17 套，含需求、架构、设计、开发、测试、部署运维、质量安全、协作管理），中文对齐 GB/T 8567、英文对齐 IEEE/ISO 标准做双轨本地化；支持三级分级范围选择、版本管理，输出 Markdown 和 Word(docx)。兼容 8 种主流 AI 工具。Invoke when user says '形成项目文档' or '生成项目文档'.
---

# 项目文档生成器（扩展版）

根据项目代码自动生成完整的软件工程文档套件，支持**中英双轨本地化**、**扩展类目**与**用户自定义范围**。

## 核心扩展点概述

1. **双轨本地化**：中文文档章节结构对齐 **GB/T 8567-2006《计算机软件文档编制规范》**；英文文档对齐 **IEEE 830 / 1016 / 1471 / 829 / ISO-IEC 25010**。每种文档按其所属标准规定章节骨架。
2. **扩展类目**：在原有 7 套基础上扩展至 **17 套**，按软件工程 7 阶段（需求→设计→开发→测试→部署运维→质量安全→协作管理）重组。
3. **三级分级范围选择**：用户在 Q3 中可从「预设组合档 / 阶段勾选 / 精确序号」三档中选择生成范围。

---

## 工作流程

### 1. 理解项目代码
- 分析项目目录结构
- 读取核心代码文件
- 理解领域模型、数据仓储、业务逻辑
- 识别设计模式和架构
- **识别目标项目技术栈与环境**（操作系统、框架、数据库、CI/CD），用于本地化环境差异块

### 2. 交互式参数收集

在生成文档前，通过交互式提问收集用户偏好。提问遵循"IDE 适配 → 三级降级 → 默认值"策略，兼容 8 种主流 IDE。

#### 2.1 IDE 提问工具映射表

| IDE | 主提问工具 | 一级降级 | 二级降级（默认值） |
|-----|------------|----------|--------------------|
| Trae | AskUserQuestion | 文本提问（编号列表） | 默认值 |
| Trae Work | AskUserQuestion | 文本提问（编号列表） | 默认值 |
| Claude Code | AskUserQuestion | 文本提问（编号列表） | 默认值 |
| Codex | 文本提问（编号列表） | 默认值 | — |
| Cursor | 文本提问（编号列表） | 默认值 | — |
| OpenCode | 文本提问（编号列表） | 默认值 | — |
| WorkBuddy | 文本提问（编号列表） | 默认值 | — |
| CodeArts | 文本提问（编号列表） | 默认值 | — |

#### 2.2 三级降级策略

1. **第1级 — 原生 AskUserQuestion**：Trae / Trae Work / Claude Code 优先调用 `AskUserQuestion` 工具，以结构化选项 chip 形式发起提问。
2. **第2级 — 文本提问（编号列表）**：当 `AskUserQuestion` 不可用或处于 Codex / Cursor / OpenCode / WorkBuddy / CodeArts 等 IDE 时，以编号列表形式输出问题到对话流，等待用户以文本回复（如"2"或"中文"）。
3. **第3级 — 默认值**：当用户回复为"跳过"/"默认"/"default"/"skip"或空回复时，使用默认值集合，并在对话流中明确提示已使用默认值。

#### 2.3 默认值集合

| 参数 | 默认值 |
|------|--------|
| 项目名称 | 从项目根目录名推断 |
| 文档语言 | 双语（中文 + 英文） |
| 本地化标准 | 双语双轨（cn→GB/T 8567 / en→IEEE 830/1016/1471/829/ISO 25010） |
| 生成范围 | 标准档（11 套，对应原 7 套语义） |
| 输出格式 | Markdown |
| 作者 | 李荣起（来自项目级 user_rules） |

#### 2.4 提问清单

依次向用户收集以下 6 项参数。每项均支持"跳过/默认/default/空回复"触发默认值。

**Q1. 项目名称确认**
- 默认：从项目根目录名推断
- 提问示例：
  ```
  Q1. 请确认项目名称（默认从目录名推断：<推断值>）：
    1) 使用推断名称
    2) 自定义名称
  请回复编号或直接输入名称（回复"默认"使用推断值）：
  ```

**Q2. 文档语言选择**
- 选项：仅中文 / 仅英文 / 双语
- 默认：双语
- 提问示例：
  ```
  Q2. 请选择文档语言：
    1) 仅中文（仅创建 cn/ 目录，章节对齐 GB/T 8567）
    2) 仅英文（仅创建 en/ 目录，章节对齐 IEEE/ISO 标准）
    3) 双语（默认，同时创建 cn/ 与 en/ 目录，双轨本地化）
  请回复编号或语言名称（回复"默认"使用双语）：
  ```

**Q3. 生成范围选择（三级分级）**
- 默认：标准档（对应原 7 套语义）
- 提问示例：
  ```
  Q3. 请选择生成范围（三级分级择一）：
    [第一级] 预设组合档：
      1) 极简档（5 套：01,02,04,06,15）
      2) 标准档（11 套：01,03,04,05,06,07,08,10,11,13,15）≈原7套语义
      3) 完整档（17 套：全生命周期全部）
    [第二级] 按软件工程阶段勾选（输入阶段号，如 "P1,P4,P5"）：
      P1需求 / P2设计 / P3开发 / P4测试 / P5部署运维 / P6质量安全 / P7协作管理
    [第三级] 精确到文档序号（如 "2,5,11"）：
      文档序号对照（阶段·中文）：
      01需求规格 02系统架构 03概要设计 04详细设计 05数据库设计 06接口契约/API
      07编码与代码审查 08测试计划 09测试用例与质量报告 10环境与配置 11部署手册
      12运维手册 13非功能需求与安全设计 14性能基准 15用户手册
      16变更管理与版本管理 17贡献指南
  请回复编号 / 阶段号 / 序号集合（回复"默认"生成标准档）：
  ```

**Q4. 输出格式确认**
- 选项：Markdown / DOCX
- 默认：Markdown
- 提问示例：
  ```
  Q4. 请选择输出格式：
    1) Markdown（默认）
    2) DOCX（需 pandoc 可用，否则回退 Markdown）
  请回复编号或格式名称（回复"默认"使用 Markdown）：
  ```
- **联动说明**：若用户选择 DOCX，对所选语言的每种文档先生成 Markdown 中间文件，再通过 pandoc 转换为 DOCX（详见"3. 输出格式判断"）。

**Q5. 本地化标准确认（仅当 Q2 选择双语或单语时联动）**
- 选项：自动双轨 / 强制单一标准 / 自定义
- 默认：自动双轨（中文→GB/T 8567；英文→IEEE/ISO）
- 提问示例：
  ```
  Q5. 请选择本地化标准：
    1) 自动双轨（默认）：按 Q2 语言映射标准（cn→GB/T 8567 / en→IEEE 830/1016/1471/829/ISO 25010）
    2) 强制单一标准：不区分语言，统一用所选标准组织章节
    3) 自定义：手动指定每种文档的章节骨架
  请回复编号或标准名（回复"默认"使用自动双轨）：
  ```

**Q6. 作者确认**
- 默认：李荣起（项目级 user_rules 优先）
- 提问示例：
  ```
  Q6. 请确认文档作者（默认：李荣起，来自项目级规则）：
    1) 使用默认作者（李荣起）
    2) 使用当前系统用户（自动获取）
    3) 使用 Git 最近提交作者（自动获取）
    4) 自定义作者名
  请回复编号或直接输入作者名（回复"默认"使用李荣起）：
  ```
- **联动说明**：作者最终取值遵循"5. 作者信息获取"中的 4 级优先级链，用户在此处的选择会作为优先级链的输入。

#### 2.5 提问结果汇总

提问完成后，在对话流中输出参数汇总表，例如：
```
✅ 参数确认：
- 项目名称：MyProject
- 文档语言：仅中文（GB/T 8567）
- 生成范围：P1,P4,P5（阶段勾选档）
- 输出格式：Markdown
- 本地化标准：自动双轨
- 作者：李荣起
```
随后进入"3. 输出格式判断"。

### 3. 输出格式判断
根据用户提示词判断文档输出格式：
- **默认**：生成 Markdown（`.md`）文件
- **触发 DOCX**：当提示词中包含以下关键词（不区分大小写）时，生成 MS Word（`.docx`）文件：
  - `docx`
  - `word`
  - `.docx`

DOCX 生成策略：
1. 先按 Markdown 模板生成完整的 Markdown 内容
2. 使用 `pandoc` 将 Markdown 转换为 DOCX：
   ```bash
   pandoc <input>.md -o <output>.docx --toc --toc-depth=3
   ```
3. 转换完成后，仅保留 `.docx` 文件（删除中间 `.md` 文件）

pandoc 不可用时的回退策略：
- 输出 Markdown 文件
- 在文档头部添加提示：`⚠️ 未检测到 pandoc，已回退为 Markdown 格式。安装 pandoc 后可使用 DOCX 输出：winget install JohnMacFarlane.Pandoc`

### 4. 版本信息获取
- 检查项目是否使用 Git 版本控制
- 提取 Git 提交记录和提交次数
- 根据当前日期生成版本号：
  - **有 Git**：版本号格式为 `1.{yy}.{Mdd}.{git提交次数}`
  - **无 Git**：版本号格式为 `1.{yy}.{Mdd}.{hmm}`（`{hmm}` 为时分）
  - 示例：`1.26.509.1234`（有 Git）、`1.26.509.1430`（无 Git）

### 5. 作者信息获取
生成文档时，作者字段按以下 4 级优先级链获取（高优先级命中即停止）：
- **第1优先级 — 项目级规则**：若 user_rules 中明确指定作者（如"作者 李荣起"），使用该作者。与 Q6 用户选择的"使用默认作者"联动。
- **第2优先级 — 当前用户**：获取调用 Skill 的用户标识（PowerShell: `$env:USERNAME`，Linux/macOS: `whoami`）。对应 Q6 选项 2。
- **第3优先级 — Git 提交作者**：从目标项目的 Git 最近一次提交中提取作者名（`git log -1 --format=%an`）。对应 Q6 选项 3。
- **第4优先级 — 置空**：若以上均无法获取，作者字段留空（`作者：` / `Author: `）

> **说明**：当用户在 Q6 中选择"自定义作者名"时，直接使用用户输入值，跳过上述优先级链。

### 6. 历史文档归档
生成新文档前，检查目标项目 `Doc/<项目名称>/` 目录下是否已存在 `cn/` 或 `en/` 子目录及文档文件。归档范围严格按用户在 Q2 选择的语言裁剪：
- **仅中文**：仅归档 `cn/` 目录；`en/`（若存在）保留不动，不参与归档
- **仅英文**：仅归档 `en/` 目录；`cn/`（若存在）保留不动
- **双语**：同时归档 `cn/` 与 `en/` 两个目录

归档操作（针对需归档的语言目录）：
- **若存在旧文档**：
  1. 读取旧文档头部信息中的版本号（`**版本**：` 字段）
  2. 在 `Doc/<项目名称>/` 下创建 `history/<旧版本号>/` 目录
  3. 将需归档的语言目录（`cn/` 和/或 `en/`）移动到 `history/<旧版本号>/` 下，保持原有子目录结构
- **若不存在旧文档**：跳过归档，直接进入下一步

### 7. 创建文档目录
根据用户在 Q2 选择的语言，在项目根目录下动态创建对应的文档子目录（如已存在则检阅现有文档作为参考）：
- **仅中文**：仅创建 `Doc/<项目名称>/cn/`
- **仅英文**：仅创建 `Doc/<项目名称>/en/`
- **双语**：同时创建 `Doc/<项目名称>/cn/` 与 `Doc/<项目名称>/en/`

### 8. 生成文档套件

#### 8.1 全生命周期文档目录（17 套 · 软件工程 7 阶段）

按用户在 Q2 选择的语言与 Q3 选择的生成范围，生成对应文档。每个阶段的中英文文档分别对齐对应标准。

| 阶段 | 序号 | 中文文档 (cn) | 英文文档 (en) | 本地化标准依据（zh / en） |
|------|------|-----------------|---------------|----------------------------|
| P1需求 | 01 | 需求规格说明书 | Requirements Specification | GB/T 8567 § / IEEE 830 |
| P2设计 | 02 | 系统架构文档 | System Architecture | GB/T 8567 / IEEE 1471·42010 |
| P2设计 | 03 | 概要设计 | Overview Design | GB/T 8567 / IEEE 1016 |
| P2设计 | 04 | 详细设计 | Detailed Design | GB/T 8567 / IEEE 1016 |
| P2设计 | 05 | 数据库设计 | Database Design | GB/T 8567 / IEEE 1016 |
| P2设计 | 06 | 接口契约 / API 文档 | Interface Contract / API | GB/T 8567 / OpenAPI |
| P3开发 | 07 | 编码规范与代码审查 | Coding Standards & Review | 团队规范 / — |
| P4测试 | 08 | 测试计划 | Test Plan | GB/T 8567 / IEEE 829 |
| P4测试 | 09 | 测试用例与质量报告 | Test Cases & QA Report | GB/T 8567 / IEEE 829 |
| P5部署 | 10 | 环境与配置文档 | Environment & Configuration | — / — |
| P5部署 | 11 | 部署手册 | Deployment Manual | — / — |
| P5部署 | 12 | 运维手册 | Operations Manual | — / — |
| P6质量 | 13 | 非功能需求与安全设计 | NFR & Security Design | GB/T 8567 / ISO-IEC 25010 |
| P6质量 | 14 | 性能基准与调优 | Performance Benchmark | GB/T 8567 / — |
| P7协作 | 15 | 用户手册 | User Manual | — / — |
| P7协作 | 16 | 变更管理与版本管理 | Change & Version Management | — / — |
| P7协作 | 17 | 贡献指南 | Contribution Guide | — / — |

**文件命名**：中文 `0X-<中文名>.md`；英文 `0X-<English Name>.md`（如 `01-需求规格说明书.md` / `01-Requirements Specification.md`）。

#### 8.2 生成规则

- **语言裁剪**：仅生成用户在 Q2 选择的语言列。仅中文 → 仅 cn/ 列；仅英文 → 仅 en/ 列；双语 → 两列均生成。
- **标准裁剪（本地化）**：每种语言的章节结构按其映射的标准组织——中文按 GB/T 8567 对应条款；英文按 IEEE/ISO 对应条款。Q5 选择"强制单一标准"时，两语言均用选定标准；Q5 选择"自定义"时按用户指定的骨架。
- **范围裁剪（三级分级）**：
  - 预设组合档：极简（01,02,04,06,15）/ 标准（01,03,04,05,06,07,08,10,11,13,15）/ 完整（01~17）
  - 阶段勾选档：按用户输入的阶段号集合展开为对应序号
  - 精确序号档：仅生成用户输入的序号
  - 所有非"完整档"均在文档头部"部分生成"标注中注明已生成与未生成的序号
- **格式联动**：当输出格式为 DOCX 时，文件扩展名由 `.md` 变为 `.docx`。

### 9. 版本差异对比
新文档生成后，若 `history/` 目录下存在旧版本文档，则逐文件对比新旧文档内容，并在每个新文档末尾追加差异日志章节。对比范围严格按用户 Q2 选择的语言裁剪：
- 仅对用户选择生成的语言目录进行新旧文档对比
- 未选择的语言不参与对比，差异日志中不出现该语言条目
- 逐文件对比新旧文档的内容差异，按新增/修改/删除分类记录

格式（中文/英文）与特殊情况处理同原规范：无差异→"与上一版本无差异"；历史缺对应文档→"全新文档"不对比；无历史版本→跳过；未选择语言不对比。

### 10. 版本管理
沿用原版本号规则（有 Git：`1.{yy}.{Mdd}.{git提交次数}`；无 Git：`1.{yy}.{Mdd}.{hmm}`）。每个文档头部含变更日志表。

### 11. 文档头部信息

每个文档必须包含：
```markdown
# 文档标题

**项目名称**：XXX项目
**作者**：<动态获取>
**日期**：YYYY-MM-DD
**版本**：1.26.509.1234
**生成范围**：全部 17 套   ← 或"部分生成（已生成：1,3,5；未生成：其余）"
**文档语言**：中文(GB/T 8567)  ← 或"英文(IEEE)" / "双语(双轨)"
**本地化标准**：GB/T 8567-2006  ← 按 Q5 选择标注

## 变更日志
...
```

字段说明：
- **作者**：通过"5. 作者信息获取"定义的 4 级优先级链动态获取。
- **生成范围**：与 Q3 选择一致。完整档标注"全部 17 套"；其余标注"部分生成"并注明序号。
- **文档语言**：与 Q2 一致；双语在语言后追加所对齐标准。
- **本地化标准**：与 Q5 一致，标注具体标准号。

### 12. 文档模板参考

详细的中英模板（按 GB/T 8567 与 IEEE/ISO 组织的章节骨架）请参阅：
- 中文模板：[references/cn/document-templates.md](references/cn/document-templates.md)（扩展版，含 17 套章节骨架）
- 英文模板：[references/en/document-templates.md](references/en/document-templates.md)（扩展版，含 17 套章节骨架）

---

## 执行检查清单

> **按用户选择动态裁剪**：以下模板在 Skill 执行时根据 Q1~Q6 用户选择动态渲染。仅渲染用户实际选择的语言、标准与文档对应检查项。

### 通用检查项（与语言/范围无关）

- [ ] 1. 项目代码已理解（架构、领域模型、设计模式、技术栈与环境）
- [ ] 2. 交互式参数收集已完成（Q1~Q6 全部收集，参数汇总表已输出）
- [ ] 3. 输出格式已判断（Markdown 或 DOCX）
- [ ] 4. 若输出 DOCX，pandoc 可用性已检查（不可用时已回退 Markdown 并提示）
- [ ] 5. 版本信息已获取（Git 提交次数或时间戳）
- [ ] 6. 作者信息已按 4 级优先级链获取
- [ ] 7. 历史文档归档已完成（仅针对用户选择的语言目录，若存在旧文档）
- [ ] 8. 本地化标准已按 Q5 确定（自动双轨 / 单一标准 / 自定义）

### 语言相关检查项（按 Q2 渲染）

**当 Q2 选择"仅中文"或"双语"时：**
- [ ] 9. `Doc/<项目名称>/cn/` 目录已创建
- [ ] 10. cn/ 文档已按 Q3 范围生成（极简/标准/完整/阶段/精确）
- [ ] 11. cn/ 文档章节结构对齐 GB/T 8567（或 Q5 选定标准）
- [ ] 12. 所有中文文档包含头部字段与变更日志

**当 Q2 选择"仅英文"或"双语"时：**
- [ ] 13. `Doc/<项目名称>/en/` 目录已创建
- [ ] 14. en/ 文档已按 Q3 范围生成
- [ ] 15. en/ 文档章节结构对齐 IEEE/ISO（或 Q5 选定标准）
- [ ] 16. 所有英文文档包含头部字段与变更日志

### 版本差异检查项（按 Q2 + 历史版本联动）

- [ ] 17. 中文版本差异对比已完成（若存在历史版本且语言含中文）
- [ ] 18. 英文版本差异对比已完成（若存在历史版本且语言含英文）
- [ ] 19. 所有对应文档包含版本差异日志（若存在历史版本）

### 部分生成检查项（仅当 Q3 非"完整档"时渲染）

- [ ] 20. 文档头部"生成范围"字段已标注"部分生成（已生成：...；未生成：...）"
- [ ] 21. 未生成的文档序号已在头部信息中明确列出