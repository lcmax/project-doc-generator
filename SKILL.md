---
name: project-doc-generator
description: 自动分析项目代码并生成7套标准文档（需求规格、概要设计、详细设计、数据库设计、API文档、测试计划、部署手册与用户手册），支持版本管理，支持输出 Markdown 和 Word(docx) 格式。Invoke when user says '形成项目文档' or '生成项目文档'.
---

# 项目文档生成器

根据项目代码自动生成完整的项目文档套件。

## 工作流程

### 1. 理解项目代码
- 分析项目目录结构
- 读取核心代码文件
- 理解领域模型、数据仓储、业务逻辑
- 识别设计模式和架构

### 2. 交互式参数收集

在生成文档前，通过交互式提问收集用户偏好。提问遵循"IDE 适配 → 三级降级 → 默认值"策略，兼容 8 种主流 IDE。

#### 2.1 IDE 提问工具映射表

| IDE | 主提问工具 | 一级降级 | 二级降级（默认值） |
|-----|------------|----------|--------------------|
| Trae | AskUserQuestion | 文本提问（编号列表） | 默认值 |
| Trae Work | AskUserQuestion | 文本提问（编号列表） | 默认值 |
| Claude Code | AskUserQuestion | 文本提问（编号列表） | 默认值 |
| Codex | 文本提问（编号列表） | 默认值 | — |
| Cursor | 文本提问（编号列表） | 默认值 | — |
| OpenCode | 文本提问（编号列表） | 默认值 | — |
| WorkBuddy | 文本提问（编号列表） | 默认值 | — |
| CodeArts | 文本提问（编号列表） | 默认值 | — |

#### 2.2 三级降级策略

1. **第1级 — 原生 AskUserQuestion**：Trae / Trae Work / Claude Code 优先调用 `AskUserQuestion` 工具，以结构化选项 chip 形式发起提问。
2. **第2级 — 文本提问（编号列表）**：当 `AskUserQuestion` 不可用或处于 Codex / Cursor / OpenCode / WorkBuddy / CodeArts 等 IDE 时，以编号列表形式输出问题到对话流，等待用户以文本回复（如"1"或"中文"）。
3. **第3级 — 默认值**：当用户回复为"跳过"/"默认"/"default"/"skip"或空回复时，使用默认值集合，并在对话流中明确提示已使用默认值。

#### 2.3 默认值集合

| 参数 | 默认值 |
|------|--------|
| 项目名称 | 从项目根目录名推断 |
| 文档语言 | 双语（中文 + 英文） |
| 生成范围 | 全部 7 套 |
| 输出格式 | Markdown |
| 作者 | 李荣起（来自项目级 user_rules） |

#### 2.4 提问清单

依次向用户收集以下 5 项参数。每项均支持"跳过/默认/default/空回复"触发默认值。

**Q1. 项目名称确认**
- 默认：从项目根目录名推断
- 提问示例：
  ```
  Q1. 请确认项目名称（默认从目录名推断：<推断值>）：
    1) 使用推断名称
    2) 自定义名称
  请回复编号或直接输入名称（回复"默认"使用推断值）：
  ```

**Q2. 文档语言选择**
- 选项：仅中文 / 仅英文 / 双语
- 默认：双语
- 提问示例：
  ```
  Q2. 请选择文档语言：
    1) 仅中文（仅创建 cn/ 目录）
    2) 仅英文（仅创建 en/ 目录）
    3) 双语（默认，同时创建 cn/ 与 en/ 目录）
  请回复编号或语言名称（回复"默认"使用双语）：
  ```

**Q3. 生成范围选择**
- 选项：全部 7 套 / 自选子集
- 默认：全部 7 套
- 提问示例：
  ```
  Q3. 请选择生成范围：
    1) 全部 7 套（默认）
    2) 自选子集（输入序号，如"1,3,5"）
  文档序号对照：
    01 需求规格 / 02 概要设计 / 03 详细设计 / 04 数据库设计
    05 API文档 / 06 测试计划 / 07 部署手册与用户手册
  请回复编号或序号集合（回复"默认"生成全部）：
  ```

**Q4. 输出格式确认**
- 选项：Markdown / DOCX
- 默认：Markdown
- 提问示例：
  ```
  Q4. 请选择输出格式：
    1) Markdown（默认）
    2) DOCX（需 pandoc 可用，否则回退 Markdown）
  请回复编号或格式名称（回复"默认"使用 Markdown）：
  ```
- **联动说明**：若用户选择 DOCX，对所选语言的每种文档先生成 Markdown 中间文件，再通过 pandoc 转换为 DOCX（详见"3. 输出格式判断"）。

**Q5. 作者确认**
- 默认：李荣起（项目级 user_rules 优先）
- 提问示例：
  ```
  Q5. 请确认文档作者（默认：李荣起，来自项目级规则）：
    1) 使用默认作者（李荣起）
    2) 使用当前系统用户（自动获取）
    3) 使用 Git 最近提交作者（自动获取）
    4) 自定义作者名
  请回复编号或直接输入作者名（回复"默认"使用李荣起）：
  ```
- **联动说明**：作者最终取值遵循"5. 作者信息获取"中的 4 级优先级链，用户在此处的选择会作为优先级链的输入。

#### 2.5 提问结果汇总

提问完成后，在对话流中输出参数汇总表，例如：
```
✅ 参数确认：
- 项目名称：MyProject
- 文档语言：仅中文
- 生成范围：1,3,5
- 输出格式：Markdown
- 作者：李荣起
```
随后进入"3. 输出格式判断"。

### 3. 输出格式判断
根据用户提示词判断文档输出格式：
- **默认**：生成 Markdown（`.md`）文件
- **触发 DOCX**：当提示词中包含以下关键词（不区分大小写）时，生成 MS Word（`.docx`）文件：
  - `docx`
  - `word`
  - `.docx`

DOCX 生成策略：
1. 先按 Markdown 模板生成完整的 Markdown 内容
2. 使用 `pandoc` 将 Markdown 转换为 DOCX：
   ```bash
   pandoc <input>.md -o <output>.docx --toc --toc-depth=3
   ```
3. 转换完成后，仅保留 `.docx` 文件（删除中间 `.md` 文件）

pandoc 不可用时的回退策略：
- 输出 Markdown 文件
- 在文档头部添加提示：`⚠️ 未检测到 pandoc，已回退为 Markdown 格式。安装 pandoc 后可使用 DOCX 输出：winget install JohnMacFarlane.Pandoc`

### 4. 版本信息获取
- 检查项目是否使用 Git 版本控制
- 提取 Git 提交记录和提交次数
- 根据当前日期生成版本号：
  - **有 Git**：版本号格式为 `1.{yy}.{Mdd}.{git提交次数}`
    - `{yy}`：年份后两位（如 2026 年为 26）
    - `{Mdd}`：月份和日期（如 5月9日 为 509）
    - `{git提交次数}`：通过 `git rev-list --count HEAD` 获取
    - 示例：`1.26.509.1234`（2026年5月9日，第1234次提交）
  - **无 Git**：版本号格式为 `1.{yy}.{Mdd}.{hmm}`
    - `{hmm}`：小时和分钟（如 14:30 为 1430）
    - 示例：`1.26.509.1430`（2026年5月9日 14:30）

### 5. 作者信息获取
生成文档时，作者字段按以下 4 级优先级链获取（高优先级命中即停止）：
- **第1优先级 — 项目级规则**：若 user_rules 中明确指定作者（如"作者 李荣起"），使用该作者。此优先级与 Q5 用户选择的"使用默认作者"联动。
- **第2优先级 — 当前用户**：获取调用 Skill 的用户标识（PowerShell: `$env:USERNAME`，Linux/macOS: `whoami`）。对应 Q5 选项 2。
- **第3优先级 — Git 提交作者**：从目标项目的 Git 最近一次提交中提取作者名（`git log -1 --format=%an`）。对应 Q5 选项 3。
- **第4优先级 — 置空**：若以上均无法获取，作者字段留空（`作者：` / `Author: `）

> **说明**：当用户在 Q5 中选择"自定义作者名"时，直接使用用户输入值，跳过上述优先级链。

### 6. 历史文档归档
生成新文档前，检查目标项目 `Doc/<项目名称>/` 目录下是否已存在 `cn/` 或 `en/` 子目录及文档文件。归档范围严格按用户在 Q2 选择的语言裁剪：
- **若用户选择仅中文**：仅归档 `cn/` 目录；`en/` 目录（若存在）保留不动，不参与归档
- **若用户选择仅英文**：仅归档 `en/` 目录；`cn/` 目录（若存在）保留不动，不参与归档
- **若用户选择双语**：同时归档 `cn/` 与 `en/` 两个目录

归档操作（针对需归档的语言目录）：
- **若存在旧文档**：
  1. 读取旧文档头部信息中的版本号（`**版本**：` 字段）
  2. 在 `Doc/<项目名称>/` 下创建 `history/<旧版本号>/` 目录
  3. 将需归档的语言目录（`cn/` 和/或 `en/`）移动到 `history/<旧版本号>/` 下，保持原有子目录结构
- **若不存在旧文档**：跳过归档，直接进入下一步

归档后目录结构示例（以双语为例，仅当用户选择该语言时归档）：
```
Doc/MyProject/
├── history/
│   └── 1.26.509.1234/        ← 旧版本号
│       ├── cn/                ← 仅当用户选择中文或双语时归档
│       │   ├── 01-需求规格.md
│       │   └── ...
│       └── en/                ← 仅当用户选择英文或双语时归档
│           ├── 01-Requirement Specification.md
│           └── ...
├── cn/                         ← 新生成的文档（仅当用户选择中文或双语时存在）
│   └── ...
└── en/                         ← 新生成的文档（仅当用户选择英文或双语时存在）
    └── ...
```

### 7. 创建文档目录
根据用户在 Q2 选择的语言，在项目根目录下动态创建对应的文档子目录（如已存在则检阅现有文档作为参考）：
- **仅中文**：仅创建 `Doc/<项目名称>/cn/`
- **仅英文**：仅创建 `Doc/<项目名称>/en/`
- **双语**：同时创建 `Doc/<项目名称>/cn/` 与 `Doc/<项目名称>/en/`

### 8. 生成文档套件

按用户在 Q2 选择的语言与 Q3 选择的生成范围，生成对应文档：

| 序号 | 中文文档 (Doc/cn/) | 英文文档 (Doc/en/) | 内容要点 |
|------|---------------------|---------------------|----------|
| 01 | 需求规格.md | 01-Requirement Specification.md | 功能概述、功能需求清单、业务规则需求、非功能需求 |
| 02 | 概要设计.md | 02-Overview Design.md | 系统架构、模块划分、类图关系、核心流程概要、依赖关系 |
| 03 | 详细设计.md | 03-Detailed Design.md | 每个方法的算法流程、分支逻辑、关键实现细节 |
| 04 | 数据库设计.md | 04-Database Design.md | 数据表结构、字段定义、表间关系、索引建议 |
| 05 | API文档.md | 05-API Documentation.md | 接口清单、入参出参、调用示例、异常场景 |
| 06 | 测试计划.md | 06-Test Plan.md | 单元测试用例、边界条件、并发测试、测试数据准备 |
| 07 | 部署手册与用户手册.md | 07-Deployment & User Manual.md | 环境要求、配置项、部署步骤、操作指南、常见问题 |

生成规则：
- **语言裁剪**：仅生成用户在 Q2 选择的语言对应的文档列。仅中文 → 仅生成 `Doc/cn/` 下列；仅英文 → 仅生成 `Doc/en/` 下列；双语 → 两列均生成。
- **范围裁剪**：
  - 若 Q3 选择"全部 7 套"：按 01~07 顺序全部生成。
  - 若 Q3 选择"自选子集"（如用户输入"1,3,5"）：仅生成对应序号文档，并在文档头部"部分生成"标注中注明已生成与未生成的序号（详见"11. 文档头部信息"）。
- **格式联动**：当输出格式为 DOCX 时，上述文件扩展名由 `.md` 变为 `.docx`（如 `01-需求规格.docx`、`01-Requirement Specification.docx`）。

### 9. 版本差异对比
新文档生成后，若 `history/` 目录下存在旧版本文档，则逐文件对比新旧文档内容，并在每个新文档末尾追加差异日志章节。对比范围严格按用户在 Q2 选择的语言裁剪：

- 仅对用户选择生成的语言目录进行新旧文档对比
- 未选择的语言不参与对比，差异日志中不出现该语言条目
- 读取 `Doc/<项目名称>/history/<旧版本号>/` 下的对应文档（仅限用户选择的语言子目录）
- 逐文件对比新旧文档的内容差异
- 在新文档末尾追加差异日志，按新增/修改/删除分类记录

差异日志格式（中文文档，仅当用户选择中文或双语时出现）：
```markdown
---

## 版本差异（v1.26.509.1234 → v1.26.510.1250）

### 🟢 新增
- 新增"四、性能监控"章节

### 🟡 修改
- 修改"二、功能需求清单"中模块B的描述

### 🔴 删除
- 删除"三、业务规则需求"中规则C
```

差异日志格式（英文文档，仅当用户选择英文或双语时出现）：
```markdown
---

## Version Diff (v1.26.509.1234 → v1.26.510.1250)

### 🟢 Added
- Added "4. Performance Monitoring" section

### 🟡 Modified
- Modified Module B description in "2. Functional Requirement List"

### 🔴 Removed
- Removed Rule C from "3. Business Rule Requirements"
```

特殊情况处理：
- **无差异**：差异日志注明"与上一版本无差异"
- **历史目录缺少对应文档**：标记为"全新文档"，不进行对比
- **无历史版本**：跳过差异对比步骤
- **未选择的语言跳过对比**：若用户选择仅中文，则 `en/` 目录（无论是否存在）均不参与对比，差异日志中不出现英文条目；反之亦然

### 10. 版本管理

#### 版本号规则
- **有 Git 版本控制**：版本号格式为 `1.{yy}.{Mdd}.{git提交次数}`
  - `{yy}`：年份后两位（如 2026 年为 26）
  - `{Mdd}`：月份和日期（如 5月9日 为 509）
  - `{git提交次数}`：通过 `git rev-list --count HEAD` 获取
  - 示例：`1.26.509.1234`（2026年5月9日，第1234次提交）
- **无 Git 版本控制**：版本号格式为 `1.{yy}.{Mdd}.{hmm}`
  - `{hmm}`：小时和分钟（如 14:30 为 1430）
  - 示例：`1.26.509.1430`（2026年5月9日 14:30）

#### Git 提交记录提取
- 使用 `git rev-list --count HEAD` 获取总提交次数
- 使用 `git log --pretty=format:"%h - %s (%an, %ar)" --date=short` 获取最近提交记录
- 提交记录用于生成变更日志和版本历史

#### 变更日志格式
在每个文档头部包含变更日志：

```markdown
## 变更日志

| 版本 | 日期 | 作者 | 变更内容 |
|------|------|------|----------|
| 1.26.509.1234 | 2026-05-09 | <动态获取> | 初始版本 |
| 1.26.510.1250 | 2026-05-10 | <动态获取> | 新增XXX章节，修正XXX描述 |
```

### 11. 文档头部信息

每个文档必须包含：

```markdown
# 文档标题

**项目名称**：XXX项目
**作者**：<动态获取>
**日期**：2026-05-09
**版本**：1.26.509.1234
**生成范围**：全部 7 套   ← 或"部分生成（已生成：1,3,5；未生成：2,4,6,7）"
**文档语言**：中文        ← 或"英文" / "双语"

## 变更日志
...
```

字段说明：
- **作者**：通过"5. 作者信息获取"中定义的 4 级优先级链动态获取，不再使用硬编码的固定作者名。优先级：项目级规则（如 user_rules 中"作者 李荣起"）→ 当前用户 → Git 提交作者 → 置空。
- **生成范围**：与 Q3 选择一致。若选择"全部 7 套"，标注"全部 7 套"；若选择"自选子集"，标注"部分生成"并注明已生成与未生成的序号。
- **文档语言**：与 Q2 选择一致，标注"中文"/"英文"/"双语"。

## 文档模板参考

详细的文档模板结构请参阅：
- 中文模板：[references/cn/document-templates.md](references/cn/document-templates.md)
- 英文模板：[references/en/document-templates.md](references/en/document-templates.md)

## 执行检查清单

> **按用户选择动态裁剪**：以下模板在 Skill 执行时根据 Q1~Q5 的用户选择动态渲染。`<lang>` 表示用户选择的语言（中文/英文/双语），`<docs>` 表示用户选择的生成范围（全部 7 套或自选子集）。仅渲染用户实际选择的语言与文档对应的检查项；未选择的语言与文档不出现检查项。

### 通用检查项（与语言/范围无关）

- [ ] 1. 项目代码已理解（架构、领域模型、设计模式）
- [ ] 2. 交互式参数收集已完成（Q1~Q5 全部收集，参数汇总表已输出）
- [ ] 3. 输出格式已判断（Markdown 或 DOCX）
- [ ] 4. 若输出 DOCX，pandoc 可用性已检查（不可用时已回退 Markdown 并提示）
- [ ] 5. 版本信息已获取（Git 提交次数或时间戳）
- [ ] 6. 作者信息已按 4 级优先级链获取（项目级规则 → 当前用户 → Git 提交作者 → 置空）
- [ ] 7. 历史文档归档已完成（仅针对用户选择的语言目录，若存在旧文档）

### 语言相关检查项（按 Q2 选择渲染）

**当 Q2 选择"仅中文"或"双语"时渲染：**
- [ ] 8. `Doc/<项目名称>/cn/` 目录已创建
- [ ] 9. cn/ 目录下文档已按 Q3 范围生成（全部 7 套或自选子集）
  - 模板（按 Q3 渲染，仅列出实际生成的序号）：
    - [ ] 01-需求规格.md 已生成
    - [ ] 02-概要设计.md 已生成
    - [ ] 03-详细设计.md 已生成
    - [ ] 04-数据库设计.md 已生成
    - [ ] 05-API文档.md 已生成
    - [ ] 06-测试计划.md 已生成
    - [ ] 07-部署手册与用户手册.md 已生成
- [ ] 10. 所有中文文档包含：项目名称、作者、日期、版本号、生成范围、文档语言
- [ ] 11. 所有中文文档包含变更日志
- [ ] 12. 中文版本差异对比已完成（若存在历史版本且选择的语言包含中文）
- [ ] 13. 所有中文文档包含版本差异日志（若存在历史版本）

**当 Q2 选择"仅英文"或"双语"时渲染：**
- [ ] 14. `Doc/<项目名称>/en/` 目录已创建
- [ ] 15. en/ 目录下文档已按 Q3 范围生成（全部 7 套或自选子集）
  - 模板（按 Q3 渲染，仅列出实际生成的序号）：
    - [ ] 01-Requirement Specification.md 已生成
    - [ ] 02-Overview Design.md 已生成
    - [ ] 03-Detailed Design.md 已生成
    - [ ] 04-Database Design.md 已生成
    - [ ] 05-API Documentation.md 已生成
    - [ ] 06-Test Plan.md 已生成
    - [ ] 07-Deployment & User Manual.md 已生成
- [ ] 16. 所有英文文档包含：项目名称、作者、日期、版本号、生成范围、文档语言
- [ ] 17. 所有英文文档包含变更日志
- [ ] 18. 英文版本差异对比已完成（若存在历史版本且选择的语言包含英文）
- [ ] 19. 所有英文文档包含版本差异日志（若存在历史版本）

### 部分生成检查项（仅当 Q3 选择"自选子集"时渲染）

- [ ] 20. 文档头部"生成范围"字段已标注"部分生成（已生成：...；未生成：...）"
- [ ] 21. 未生成的文档序号已在头部信息中明确列出
